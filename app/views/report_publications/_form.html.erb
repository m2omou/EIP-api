<%= form_for(@report_publication) do |f| %>
  <% if @report_publication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_publication.errors.count, "error") %> prohibited this report_publication from being saved:</h2>

      <ul>
      <% @report_publication.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :publication_id %><br>
    <%= f.number_field :publication_id %>
  </div>
  <div class="field">
    <%= f.label :reason %><br>
    <%= f.number_field :reason %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
