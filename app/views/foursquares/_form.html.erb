<%= form_for(@foursquare) do |f| %>
  <% if @foursquare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@foursquare.errors.count, "error") %> prohibited this foursquare from being saved:</h2>

      <ul>
      <% @foursquare.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
